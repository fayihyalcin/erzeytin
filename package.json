{
  "name": "zeytin",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c cyan,magenta \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev -- --host 0.0.0.0 --port 5173\"",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "npm --prefix frontend run dev -- --host 0.0.0.0 --port 5173",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "db:generate": "npm --prefix backend run db:generate",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:baseline": "npm --prefix backend run db:baseline"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
